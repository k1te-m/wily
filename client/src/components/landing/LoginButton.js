import React from "react";
import styled from "styled-components";
import { motion } from "framer-motion";

const LoginBtn = styled(motion.button)``;

const LoginButton = () => {
  return <LoginBtn>Log in</LoginBtn>;
};

export default LoginButton;
