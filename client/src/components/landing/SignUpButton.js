import React from "react";
import styled from "styled-components";
import { motion } from "framer-motion";

const SignUpBtn = styled(motion.button)``;

const SignUpButton = () => {
  return <SignUpBtn>Sign up</SignUpBtn>;
};

export default SignUpButton;
